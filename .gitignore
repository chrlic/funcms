# Nuxt build artifacts
.nuxt/
.output/
dist/

# Node
node_modules/

# Env — keep .env.example, never commit secrets
.env
.env.local

# Git worktrees — session branches, ephemeral
# (content repo lives at ../funcms-content, outside this tree)
.worktrees/

# Safety net: if CONTENT_DIR is ever misconfigured to point inside the app dir
content/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Logs
*.log
funcms.code-workspace
